#!/usr/bin/env sh
if ! command -v pnpm >/dev/null 2>&1; then
  echo "⚠️  pnpm not found in PATH — skipping pre-push hooks"
  echo "   Run 'pnpm run test:unit' manually before pushing."
  exit 0
fi

pnpm run test:unit
