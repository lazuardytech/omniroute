# @lobehub/icons declares UI peers that are not needed by our deep icon imports.
# Keeping peer auto-install disabled prevents unused UI peers from being pulled
# back into the tree and reopening audit findings for unused packages.
legacy-peer-deps=true
